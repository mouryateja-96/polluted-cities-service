# Server
PORT=3000
NODE_ENV=development

# External Mock Pollution API
POLLUTION_API_BASE=https://be-recruitment-task.onrender.com
POLLUTION_API_USERNAME=testuser
POLLUTION_API_PASSWORD=testpass

# (Optional) Override the data endpoint if docs differ.
# The service will try several candidates if this is not set.
# POLLUTION_API_PATH=/pollution/cities

# Wikipedia REST API
WIKIPEDIA_API_BASE=https://en.wikipedia.org/api/rest_v1
# Per Wikipedia etiquette include a descriptive UA with contact.
WIKIPEDIA_USER_AGENT=PollutedCitiesService/1.0 (example@example.com)
